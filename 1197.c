/* Kauan Dias Caisley AED 1 Volta à Faculdade de Física */

#include <stdio.h>

int main() {
    int v, t;

    while (scanf("%d %d", &v, &t) != EOF) {
        printf("%d\n", 2 * v * t); // essa foi dificil. 
    }

    return 0;
}
